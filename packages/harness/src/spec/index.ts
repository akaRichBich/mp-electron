/**
 * The spec, on its own. Imported by the browser form as well as the CLI, so it
 * must stay free of node - which `pnpm check:arch` now enforces.
 */
export * from './rule-spec'
export * from './render-prompt'
